# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="rfid_uart",  # Must be unique
    name="RFID Authenticator",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="rfid_uart_app",
    stack_size=2 * 1024,
    fap_category="GPIO",
    # Optional values
    # fap_version="0.1",
    # fap_description="A simple app",
    # fap_author="J. Doe",
    # fap_weburl="https://github.com/user/template"
)
